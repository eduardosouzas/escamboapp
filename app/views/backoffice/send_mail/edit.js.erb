$(function(){
var modal = $('#sendEmailModal');
    modal.find('.modal-title').text('Nova mensagem para <%= @admin.name%>');
    modal.find('input').val('<%= @admin.email %>');

    modal.modal();
    $('#sendEmailModal').on('show.bs.modal', function (event) {
        $('#message-text').focus();
    });
});
